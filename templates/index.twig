{% extends "layout.twig" %}
{% block content %}

    <!-- Main Content -->
    <div class="container">
      <div class="row">
        <div class="col-lg-8 col-md-10 mx-auto">
          <!-- Posts -->
          {% for post in posts %}
            <div class="post-preview">
                <a href="/index.php?controller=Blog&action=renderPost&id={{ post.id }}">
                  <h2 class="post-title text-center">{{ post.title }}</h2>
                </a>
                <p>
                  {{ post.content }}
                </p>
                <p class="post-meta">Posté par
                  <a href="#">Joffrey</a>
                  le {{ post.createdAt }}</p>
            </div>
            <hr>
          {% endfor %}
          <!-- Pager -->
          <div class="clearfix">
            <a class="btn btn-secondary float-right" href="/index.php?controller=Blog&action=renderPosts">Voir tous les posts &rarr;</a>
          </div>
        </div>
      </div>
    </div>

    <hr>

{% endblock %}
